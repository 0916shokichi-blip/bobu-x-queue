name: submit pending tweets

on:
  schedule:
    # シリーズ 22:00 JST 投稿の確実化 (UTC 13:00-13:30, 5分間隔で 7 attempts)
    - cron: '0,5,10,15,20,25,30 13 * * *'
    # 非 22:00 帯の安全網 (3 時間毎)
    - cron: '0 */3 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: x-queue-write
  cancel-in-progress: false

jobs:
  submit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Print auth fingerprint (rotate divergent 検出用)
        env:
          X_CONSUMER_KEY: ${{ secrets.X_CONSUMER_KEY }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
        run: |
          echo "auth fingerprint (compare with Mac \`x status\`):"
          echo "  X_CONSUMER_KEY[:8]: ${X_CONSUMER_KEY:0:8}..."
          echo "  X_ACCESS_TOKEN[:8]: ${X_ACCESS_TOKEN:0:8}..."

      - name: Submit pending tweets
        env:
          X_QUEUE_ROOT: ${{ github.workspace }}/queue
          X_LOG_DIR: ${{ github.workspace }}/logs
          X_CONSUMER_KEY: ${{ secrets.X_CONSUMER_KEY }}
          X_CONSUMER_SECRET: ${{ secrets.X_CONSUMER_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          X_HANDLE: bobu_reflect
        run: python scripts/x-submit.py

      - name: Backfill vault export (idempotent safety net)
        env:
          X_QUEUE_ROOT: ${{ github.workspace }}/queue
          X_VAULT_EXPORT_ROOT: ${{ github.workspace }}/vault-export
        run: python scripts/vault_export.py backfill

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add queue/ vault-export/
          if ! git diff --cached --quiet; then
            git commit -m "bot/submit: process pending queue [skip ci]"
            for i in 1 2 3; do
              git pull --rebase --autostash && git push && break
              echo "push retry $i"
              sleep 5
            done
          else
            echo "no changes to commit"
          fi
